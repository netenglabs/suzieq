- namespace: native-ns
  devices:
  - hostname: device0
    username: username
    keyfile: tests/unit/poller/controller/credential_loader/cred_file/data/keyfile
  - hostname: device1
    key-passphrase: my-pass
    username: vagrant
    keyfile: tests/unit/poller/controller/credential_loader/cred_file/data/keyfile
  - hostname: device2
    username: vagrant
    password: password
