inventory_provider:
  plugin_type:
    inventorySource:
    - local-netbox:
      plugin_name: netbox
      args:
        credentials:
          token: "0123456789abcdef0123456789abcdef01234567"
          # username: admin
          # password: admin
        ip_address: "127.0.0.1"
        use_https: false
        port: "8000"
        namespace: "local"
        tag: "suzieq"
        period: 60
        device_credentials:
          type: credfile
          file_path: "/home/luca/stardust/suzieq/suzieq/inventoryProvider/plugins/inventorySource/examples/local-netbox-devices-credentials.yaml"
    - remote-netbox-demo:
      plugin_name: netbox
      args:
        credentials:
          username: "admin"
          password: "admin"
        ip_address: "demo.netbox.dev"
        use_https: true
        port: "443"
        tag: "test-tag"
        device_credentials:
          skip: true
          # file_path: "./remote-netbox-demo-devices-credentials.yaml"
#   pollerManager:
#     ...
#   chunker:
#     ...
# provider_config:
#   period: 60
#   ...
    