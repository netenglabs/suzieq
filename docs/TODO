- Display times in local time since all times are stored in UTC.
  - Allow user to specify TZ (or use what's the local machine's)

- OSPF Troubleshooting issues
  - no announcement of ip addr due to lo not assigned
  - rp_filter not 0
  - addr mismatch, /24 vs /32
  - 
  