- Unique router ID (OSPF)

  df.groupby('routerId', as_index=False)[['hostname']].agg(lambda x: x.unique().tolist())

* Search if IPAddress exists in list
  df[df.ipAddressList.apply(lambda x: '10.0.0.112/32' in x)]

  errors out with these choices: 0.24.2 & 3.7
  df.query('"10.0.0.112/32" == ipAddressList')
  df.query('"10.0.0.112/32" in ipAddressList')

* 
